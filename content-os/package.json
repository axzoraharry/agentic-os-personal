{
  "name": "content-os",
  "version": "0.1.0",
  "description": "Agentic content OS: scrape RSS + URLs, AI-rank by priority, generate posts, and schedule to Instagram, LinkedIn, and X via Zernio.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node --check server.js && node --check app.js && node --check server/index.js && node --check server/db.js && node --check server/sqlite.js && node --check server/agent/auth.js && node --check server/agent/tools.js && node --check server/agent/validate.js && node --check server/agent/router.js && node --check scripts/agent-cli.js && echo BUILD_OK",
    "dev": "node --watch server/index.js",
    "test:apis": "node scripts/test-apis.js",
    "agent": "node scripts/agent-cli.js",
    "setup:termux": "bash deploy/termux-setup.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "googleapis": "^173.0.0",
    "node-sqlite3-wasm": "^0.8.59",
    "p-queue": "^8.1.0",
    "rss-parser": "^3.13.0"
  }
}
